# Formex — backend (PostgreSQL + Redis + Directus)
# Скопируйте в .env и задайте свои значения: cp .env.example .env

# === PostgreSQL ===
POSTGRES_USER=directus
POSTGRES_PASSWORD=directus_secret
POSTGRES_DB=directus

# === Directus ===
DIRECTUS_PORT=8055
DIRECTUS_KEY=change-me-to-random-32-chars-minimum
DIRECTUS_SECRET=change-me-to-random-32-chars-minimum
DIRECTUS_ADMIN_EMAIL=admin@formex.kg
DIRECTUS_ADMIN_PASSWORD=12345678

# Публичный URL админки (для писем и ссылок)
PUBLIC_URL=http://localhost:8055

# Лимит загрузки файлов (по умолчанию 50mb)
# FILES_MAX_UPLOAD_SIZE=50mb

# Для продакшена укажите реальный домен, например:
# PUBLIC_URL=https://cms.formex.kg

# === Bootstrap (опционально) ===
# URL Directus для скрипта bootstrap
DIRECTUS_URL=http://localhost:8055
# Токен: создайте в админке Settings → Access Tokens, затем вставьте сюда
# DIRECTUS_ADMIN_TOKEN=your-token-here

# === Flow: автоэкспорт в GitHub при сохранении (setup-flow-github.mjs) ===
# GITHUB_PAT=ghp_xxx   # Personal Access Token (scope: repo)
# GITHUB_REPO=n0b1esse/formex

# === Realtime (WebSocket для мгновенного обновления на фронте) ===
# URL webhook для Directus Flow. В Docker: http://realtime:3001/webhook
# REALTIME_WEBHOOK_URL=http://realtime:3001/webhook
