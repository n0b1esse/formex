# Formex — backend (PostgreSQL + Redis + Directus)
# Скопируйте в .env и задайте свои значения: cp .env.example .env

# === PostgreSQL ===
POSTGRES_USER=directus
POSTGRES_PASSWORD=directus_secret
POSTGRES_DB=directus

# === Directus ===
DIRECTUS_PORT=8055
DIRECTUS_KEY=change-me-to-random-32-chars-minimum
DIRECTUS_SECRET=change-me-to-random-32-chars-minimum
DIRECTUS_ADMIN_EMAIL=admin@formex.kg
DIRECTUS_ADMIN_PASSWORD=12345678

# Публичный URL админки (для писем и ссылок)
PUBLIC_URL=http://localhost:8055

# Для продакшена укажите реальный домен, например:
# PUBLIC_URL=https://cms.formex.kg
